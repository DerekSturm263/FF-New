component ItemInstance
{
    asset_ref<Item> Item;

    entity_ref Owner;
    entity_ref Holder;
    entity_ref Target;
    
    bool FallState;
    bool IsActive;
    FP FallSpeed;
    FP FallY;
    FP ActiveTime;

    int Uses;
}
